# IntentRAG v2 — Production Requirements (no PyTorch, ~150MB total)
# For memory-constrained platforms (Render free tier: 512MB)
# Uses heuristic intent classifier + FastEmbed (ONNX) for embeddings

# API Layer
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
greenlet>=3.0.0

# Vector Store + Embeddings
qdrant-client>=1.9.0
llama-index-core>=0.10.0
llama-index-vector-stores-qdrant>=0.2.0
llama-index-embeddings-fastembed>=0.1.0
fastembed>=0.3.0

# LLM (Groq via OpenAI SDK)
openai>=1.30.0

# Reranking
cohere>=5.0.0

# Observability
langfuse>=2.36.0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
email-validator>=2.1.0

# Web search fallback
ddgs>=7.0.0

# PDF parsing
pymupdf>=1.24.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
numpy>=1.24.0,<2.0.0
